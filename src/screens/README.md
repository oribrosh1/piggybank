# Screens

Full-screen UI lives here. Each screen has:
- `ScreenName.tsx` – presentational component
- `useScreenName.ts` – state, API calls, handlers (optional)

Route files in `app/` import from `@/src/screens/...` and re-export.
